# GoldMem Roadmap

本文件是中文总览。权威的分阶段路线以 `docs/roadmap/*` 为准，尤其是
`docs/roadmap/09-production-graphiti-plan-pack.md`。

GoldMem 的目标不是“老人语音备忘录”，而是一个多租户、证据绑定、可持续整理的老人照护记忆系统。

核心路线：

```text
PostgreSQL / Citus-ready = 业务、证据、权限、审计 truth
Mem0 = 中短期 multilingual recall engine
Graphiti = 长期关系记忆 truth
Memory Kernel = 编排、安全裁决、证据融合、业务动作控制
Nightly Consolidation + Eval = 数据飞轮
```

## 当前代码基线

当前代码已经具备第一版 Memory Kernel MVP：

- `packages/memory-schema` 定义 MemoryPlan、source、event、reminder、risk、family task、context link、answer 等 domain schema。
- `packages/memory-kernel` 负责 ingest、MemoryPlan validation、risk/permission/reminder guardrails、PostgreSQL persistence、Mem0 write、recall evidence merge。
- `packages/memory-store` 提供 PostgreSQL truth adapter 和 Mem0 HTTP adapter。
- `packages/model-gateway` 提供 OpenAI-compatible MemoryPlan/query/answer boundary。
- `packages/temporal-memory` 已建立 Graphiti 目标接口和 Null store。
- `services/api-server` 提供 thin Fastify API。
- `apps/web-mvp` 是中文优先的单页 Web MVP。
- `evals` 和 `e2e/*` 已覆盖 recall、risk、context link 和 Mem0 evidence boundary。

已经成立的原则：

- LLM 理解，Kernel 约束。
- PostgreSQL 是业务和证据 truth。
- Mem0 负责多信号召回候选，不做事实裁决。
- Graphiti 负责长期关系记忆，不触发业务动作。
- Recall answer 必须 evidence-bound。
- Context links 是 MVP 轻量关系线索，不是最终长期事实图。
- 中文是默认产品语言。

## 分层职责

### PostgreSQL

PostgreSQL 持有确定性的业务和证据状态：

- tenants / users / elder ownership
- sources / events
- reminders
- risk flags
- family tasks
- context links
- feedback
- audit logs
- export/delete/RLS/Citus-ready tenant boundary

PostgreSQL 不再承担“自建完整长期事实图”的终局职责。它仍然是所有业务动作、权限、安全和证据回查的硬依赖。

### Mem0

Mem0 是中短期多语言召回引擎。GoldMem 应尽量利用 Mem0 的：

- semantic search
- BM25 / keyword recall
- entity linking
- rerank
- dedup / context lookup
- alias / preference / fuzzy context

但 Mem0 输出只能作为 candidate 和 ranking signal，不能成为 GoldMem truth。

中文策略：

- OpenAI embedding 路径可以支持中文语义召回。
- hybrid/BM25/entity NLP 对中文可能不稳定，所以应增强自托管 Mem0 的中文分词、领域词典、entity recall 和 rerank。
- 不应把 Mem0 降级成纯向量搜索，也不应让 Mem0 直接裁决事实。
- canonical event memory 写入保持 `infer=false`，metadata 必须带 `tenantId/sourceId/eventId`。
- recall 结果必须回查 PostgreSQL，final evidence 优先来自 PostgreSQL 记录或 PostgreSQL-derived metadata。

### Graphiti

Graphiti 是长期关系记忆 truth 的目标生产链路，负责：

- entities
- relationships
- temporal facts
- validity windows
- supersession
- relationship history
- source/episode provenance

Graphiti 不能替代 PostgreSQL 的业务 truth，也不能直接调度提醒、通知家人、改变权限、确认风险或写业务状态。所有用户可见答案和业务动作仍由 Kernel 融合证据后裁决。

### Kernel

Kernel 是唯一编排层，负责：

- MemoryPlan 校验
- risk / permission / reminder guardrails
- PostgreSQL writes
- Mem0 recall write/search
- Graphiti episode write/search
- evidence alignment and ranking
- no-evidence no-answer
- high-risk uncertainty
- audit

API routes 和前端必须保持 thin，不能复制后端规则，也不能直接调用 Mem0 或 Graphiti。

## 推进顺序

```text
1. 修复当前验证问题，确保 pnpm mvp:verify 绿色。
2. 统一文档方向，以 docs/roadmap/* 为主路线。
3. 补 tenantId 全链路，所有 store/API/backend call 都 tenant-aware。
4. 实现 GraphitiTemporalMemoryStore HTTP adapter。
5. Graphiti 写入进入 ingest 生产链路。
6. Graphiti evidence 进入 queryMemory。
7. 建立 Graphiti production golden cases。
8. Nightly Consolidation 强化 Graphiti 长期记忆。
9. Admin debugger / eval flywheel。
```

## 验收原则

- Graphiti 可以成为长期关系记忆 truth，但 PostgreSQL 仍是业务、证据、权限和审计 truth。
- Graphiti 失败不能腐蚀 PostgreSQL truth。
- Graphiti evidence 必须携带 `tenantId/elderId/groupId/sourceId/eventId/episodeId` 等可对齐元数据。
- 高风险回答必须保留不确定性和证据来源。
- 每个生产链路能力都必须有 golden case，不用等待大批量 case 才接入，但不能无验证接入。

## Required Verification

广泛变更：

```bash
pnpm typecheck
pnpm test
pnpm build
pnpm lint
pnpm eval
pnpm architecture:check
```

MVP confidence：

```bash
pnpm mvp:verify
pnpm mvp:smoke
pnpm e2e:golden
```

Graphiti 生产链路新增能力还需要对应 golden/e2e case。真实 query 或模型失败必须转化为 regression test、eval case 或 golden fixture。
