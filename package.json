{
  "name": "goldmem",
  "version": "0.1.0",
  "private": true,
  "description": "Elder-first AI memory and reminder system.",
  "packageManager": "pnpm@9.15.4",
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "tsx": "^4.19.4",
    "zod": "^3.24.4"
  }
}
