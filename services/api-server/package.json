{
  "name": "@goldmem/api-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: api-server'",
    "test": "echo 'test: api-server'",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@goldmem/memory-kernel": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
