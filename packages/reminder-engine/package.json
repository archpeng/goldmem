{
  "name": "@mem/reminder-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mem/memory-schema": "workspace:*",
    "@mem/memory-store": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
