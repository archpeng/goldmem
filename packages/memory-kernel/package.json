{
  "name": "@mem/memory-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mem/memory-schema": "workspace:*",
    "@mem/model-gateway": "workspace:*",
    "@mem/memory-store": "workspace:*",
    "@mem/temporal-memory": "workspace:*",
    "@mem/reminder-engine": "workspace:*",
    "@mem/risk-engine": "workspace:*",
    "@mem/permission-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
